package com.pacxon.states;


/*tirar*/




public class GameStateTest {
/*
    //Verifica se o modelo associado ao Viewer é o mesmo Arena criado anteriormente
    @Test
    void testGetViewer() {
        Arena arena = new Arena(5, 5); // Cria uma instância fictícia de Arena
        GameState gameState = new GameState(arena);

        Viewer<Arena> viewer = gameState.getViewer();

        assertNotNull(viewer);
        assertTrue(viewer instanceof GameViewer);
        assertEquals(arena, viewer.getModel());
    }


    //Verifica se o modelo associado ao Controller é o mesmo Arena criado anteriormente
    @Test
    void testGetController() {
        Arena arena = new Arena(5, 5); // Cria uma instância fictícia de Arena
        GameState gameState = new GameState(arena);

        Controller<Arena> controller = gameState.getController();

        assertNotNull(controller);
        assertTrue(controller instanceof ArenaController);
        assertEquals(arena, controller.getModel());
    }

 */

}


