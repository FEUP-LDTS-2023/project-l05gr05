package com.pacxon.model;

import org.junit.jupiter.api.Test;
import java.io.IOException;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;


/*tirar*/

public class LoaderArenaBuilderTest {
    //Verifica se a criação da arena usando o LoaderArenaBuilder gera uma arena não nula, com listas de monstros e paredes não nulas, e um Pacman não nulo
    /* @Test
    void testCreateArena() throws IOException {
        ArenaBuilder arenaBuilder = new LoaderArenaBuilder(1);
        Arena arena = arenaBuilder.createArena();

        assertNotNull(arena);
        assertNotNull(arena.getMonsters());
        assertNotNull(arena.getWalls());
        assertNotNull(arena.getPacman());

        // Por completar
    }

    //Verifica se o método getWidth retorna a largura esperada com base no nível carregado
    @Test
    void testGetWidth() throws IOException {
        ArenaBuilder arenaBuilder = new LoaderArenaBuilder(1);
        int width = arenaBuilder.getWidth();
        // Por completar
    }


    //Verifica se o método getHeight retorna a altura esperada com base no nível carregado
    @Test
    void testGetHeight() throws IOException {
        ArenaBuilder arenaBuilder = new LoaderArenaBuilder(1);
        int height = arenaBuilder.getHeight();

        // Por completar
    }

    //Verifica se o método createWalls retorna a lista de paredes esperada com base no nível carregado
    @Test
    void testCreateWalls() throws IOException {
        ArenaBuilder arenaBuilder = new LoaderArenaBuilder(1);
        List<Wall> walls = arenaBuilder.createWalls();

        // Por completar
    }

    //Verifica se o método createMonsters retorna a lista de monstros esperada com base no nível carregado
    @Test
    void testCreateMonsters() throws IOException {
        ArenaBuilder arenaBuilder = new LoaderArenaBuilder(1);
        List<Monster> monsters = arenaBuilder.createMonsters();

        // Por completar
    }


    //Verifica se o método createPacman retorna o Pacman esperado com base no nível carregado
    @Test
    void testCreatePacman() throws IOException {
        ArenaBuilder arenaBuilder = new LoaderArenaBuilder(1);
        Pacman pacman = arenaBuilder.createPacman();
        // Por completar
    }

     */
}
