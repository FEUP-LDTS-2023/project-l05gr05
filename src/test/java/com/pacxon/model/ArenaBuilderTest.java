package com.pacxon.model;

import org.junit.jupiter.api.Test;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

/*public class ArenaBuilderTest {
    //testar se a criação de uma arena usando o ArenaBuilder é feita corretamente
    @Test
    void testCreateArena() {
        ArenaBuilder arenaBuilder = new MockArenaBuilder();
        Arena arena = arenaBuilder.createArena();

        assertNotNull(arena);
        assertNotNull(arena.getMonsters());
        assertNotNull(arena.getWalls());
        assertNotNull(arena.getPacman());

        // Por completar
    }

    //uma implementação fictícia do ArenaBuilder para fins de teste
    private static class MockArenaBuilder extends ArenaBuilder {
        @Override
        protected int getWidth() {
            return 26; // possivelente alterar largura
        }

        @Override
        protected int getHeight() {
            return 21; // possivelente alterar altura
        }

        @Override
        protected List<Wall> createWalls() {
            //por completar
            return List.of(new Wall(0, 0), new Wall(2, 2), new Wall(4, 4));
        }

        @Override
        protected List<Monster> createMonsters() {
            //por completar
            return List.of(new Monster(1, 1), new Monster(3, 3));
        }

        @Override
        protected Pacman createPacman() {
            //por completar
            return new Pacman(0, 2);
        }
    }


}*/
